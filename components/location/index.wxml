<view class="lineup-stage-location-wrap">
    <block wx:if="{{location.length}}">
        <view class="lineup-location">
            <block wx:for="{{location}}" wx:key="item.id" wx:for-index="idx" wx:for-item="item">
                <view class="location-champion position{{item.location}}">
                    <view class="bg-img" style="background-image: url('{{item.hero.avatar}}');"></view>
                    <block wx:if="{{detail}}">
                        <view wx:if="{{item.is_carry_hero}}" class="icon-carry"></view>
                        <block wx:if="{{item.equipment_data.length}}">
                            <view class="equipment-list">
                                <block wx:for="{{item.equipment_data}}" wx:key="equipment.id" wx:for-item="equipment" wx:for-index="e">
                                    <l-avatar
                                            l-class="component-equipment"
                                            size="30"
                                            src="{{equipment.image}}"/>
                                </block>
                            </view>
                        </block>
                    </block>
                </view>
            </block>
        </view>
    </block>
</view>
